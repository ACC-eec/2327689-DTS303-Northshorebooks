# Copy this file to .env and update with your settings

# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_NAME=northshore_books
DB_USER=root
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=3306

# Database Type (set to True only for SQLite-only local development)
# Submission default is False so the marker uses MySQL per the brief.
USE_SQLITE=False

# Security Settings (set to True in production with HTTPS)
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
